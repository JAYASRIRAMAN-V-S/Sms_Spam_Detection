# Sms_Spam_Detection
This project uses a Naive Bayes classifier to detect spam messages from the spam.csv dataset. It includes text preprocessing with NLTK, vectorization with CountVectorizer, and model training with scikit-learn. Results are evaluated using accuracy metrics and a confusion matrix. Licensed under MIT.
